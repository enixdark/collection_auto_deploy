apiVersion: v1
kind: Service
metadata:
  name: {{account['name']}}
  namespace: {{namespace}}
spec:
  ports:
    - name: {{account['name']}}
      port: {{account['port']}}
  externalIPs:
    - {{account['host']}}
  selector:
    app: {{account['name']}}

