apiVersion: v1
kind: Service
metadata:
  name: {{rabbitmq['name']}}
  namespace: {{namespace}}
spec:
  ports:
    - name: {{rabbitmq['name']}}
      port: {{rabbitmq['port']}}
  externalIPs:
    - {{rabbitmq['host']}}
  selector:
    app: {{rabbitmq['name']}}

